import { FadeInOutDirective } from './fade-in-out.directive';

describe('FadeInOutDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInOutDirective();
    expect(directive).toBeTruthy();
  });
});
