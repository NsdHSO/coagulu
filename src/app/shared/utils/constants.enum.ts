export enum ConstantsEnum {
  ZERO,
}

export enum TypeConstantEnum {
  EMAIL = 'email',
  MAX = 'max',
  MIN = 'min',
  NUMBER = 'number',
  PATTERN = 'pattern',
  REQUIRED = 'required',
  STRING = 'string',
  MIN_CHAR = 'minChar',
}
