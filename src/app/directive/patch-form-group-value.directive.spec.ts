import { PatchFormGroupValueDirective } from './patch-form-group-value.directive';

describe('PatchFormGroupValueDirective', () => {
  it('should create an instance', () => {
    const directive = new PatchFormGroupValueDirective();
    expect(directive).toBeTruthy();
  });
});
