import { PapControlDirective } from './pap-control.directive';

describe('PapControlDirective', () => {
  it('should create an instance', () => {
    const directive = new PapControlDirective();
    expect(directive).toBeTruthy();
  });
});
