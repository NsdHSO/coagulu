import { ScollHintDirective } from './scoll-hint.directive';

describe('ScollHintDirective', () => {
  it('should create an instance', () => {
    const directive = new ScollHintDirective();
    expect(directive).toBeTruthy();
  });
});
