import { ScrollHintDirective } from './scroll-hint.directive';

describe('ScollHintDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHintDirective();
    expect(directive).toBeTruthy();
  });
});
