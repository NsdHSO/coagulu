import { IncrementZindexDirective } from './increment-zindex.directive';

describe('IncrementZindexDirective', () => {
  it('should create an instance', () => {
    const directive = new IncrementZindexDirective();
    expect(directive).toBeTruthy();
  });
});
