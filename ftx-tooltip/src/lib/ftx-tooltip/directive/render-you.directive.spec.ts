import { RenderYouDirective } from './render-you.directive';

describe('RenderYouDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderYouDirective();
    expect(directive).toBeTruthy();
  });
});
