import { RetrievePlaceholderPipe } from './retrieve-placeholder.pipe';

describe('RetrievePlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new RetrievePlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
