import { GetValueToShowPipe } from './get-value-to-show.pipe';

describe('GetValueToShowPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueToShowPipe();
    expect(pipe).toBeTruthy();
  });
});
