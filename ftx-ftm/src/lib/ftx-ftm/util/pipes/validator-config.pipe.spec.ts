import { ValidatorConfigPipe } from './validator-config.pipe';

describe('ValidtorConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidatorConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
